version: "2.0"

rules:
- rule: fallback wikipedia
  steps:
  - intent: nlu_fallback
  - action: action_default_fallback

- rule: analizar requerimientos
  steps:
  - intent: requerimiento
  - action: action_modelado